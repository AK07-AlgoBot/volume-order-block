# Optional override. In `npm run dev`, the app defaults to http://127.0.0.1:8080
# (same port as a typical local uvicorn + Kite redirect http://127.0.0.1:8080/kite/callback).
# VITE_DASHBOARD_API_BASE=http://127.0.0.1:8080
