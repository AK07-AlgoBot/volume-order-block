# Optional override. In `npm run dev`, the app defaults to http://127.0.0.1:8000.
# Set this if the API is on another host/port, or for `npm run preview` without a proxy.
# VITE_DASHBOARD_API_BASE=http://127.0.0.1:8000
