# Optional: force API URL (bypasses Vite proxy). Leave unset for local dev — requests go to
# same origin (:5173) and Vite proxies /api and /ws to http://127.0.0.1:8080.
# VITE_DASHBOARD_API_BASE=http://127.0.0.1:8080
